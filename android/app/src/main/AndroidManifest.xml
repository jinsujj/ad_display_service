<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.owldev.adsignage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AdSignage"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false">

        <!--
            AC 11: MainActivity는 https://stream.owl-dev.me/player/{deviceId}를
            가리키는 전체화면 WebView를 immersive sticky 모드로 실행한다.

            - 테마 @style/Theme.AdSignage가 윈도우 레벨에서 상태바/내비게이션바를
              미리 제거하므로 콜드 스타트 첫 프레임이 완전히 검정이다
            - launchMode singleTask + clearTaskOnLaunch=false 조합으로 디바이스
              수명 동안 WebView가 상주하도록 유지한다
            - screenOrientation=portrait — 시드 사양에 따라 키오스크 냉장고
              디스플레이는 10–15" 세로 FHD다
            - configChanges로 회전/키보드 이벤트를 가로채 재생 중에 WebView가
              파괴되지 않도록 한다
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AdSignage"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode|smallestScreenSize|screenLayout"
            android:resizeableActivity="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            AC 13: 디바이스 부팅 시 사이니지 플레이어 자동 시작.

            Stock Android는 사용자가 디바이스를 처음 잠금 해제한 뒤
            BOOT_COMPLETED를 발사한다. 저가 냉장고 부착형 Android 박스에서
            흔히 쓰이는 일부 OEM ROM(HTC, Xiaomi, Samsung, 일반 AOSP 퀵부트
            커널 등)은 자체 부팅 브로드캐스트를 함께 송출하므로, 벤더에 관계
            없이 매 전원 사이클 후 WebView 래퍼가 다시 올라오도록 그 신호도
            함께 수신한다.

            리시버는 기본(중간) 우선순위로 동작하며 RECEIVE_BOOT_COMPLETED로
            게이팅되어 OS만 해당 인텐트를 전달할 수 있다.
        -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.REBOOT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
